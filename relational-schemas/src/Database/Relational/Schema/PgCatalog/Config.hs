module Database.Relational.Schema.PgCatalog.Config
  {-# DEPRECATED "import config from Database.Relational.Schema.PostgreSQL instead of this module." #-} (
  config,
  ) where

import Database.Relational.Schema.PostgreSQL.Config (config)
