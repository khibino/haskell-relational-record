module Database.Relational.Schema.MySQLInfo.Config
  {-# DEPRECATED "import config from Database.Relational.Schema.MySQL instead of this module." #-} (
  config,
  ) where

import Database.Relational.Schema.MySQL.Config (config)
